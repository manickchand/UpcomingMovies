<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manickchand.upcomingmovies">

    <application
        android:name="com.manickchand.upcomingmovies.BaseApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_logo_movies"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo_movies"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.searchList.SearchListActivity"></activity>
        <activity
            android:name=".ui.movieDetail.MovieDetailActivity"
            android:label="@string/title_activity_movie_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>